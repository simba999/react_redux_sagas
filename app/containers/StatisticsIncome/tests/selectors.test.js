// import { selectStatisticsIncomeDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectStatisticsIncomeDomain();
describe('selectStatisticsIncomeDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
