// import { selectSettingsAccountPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectSettingsAccountPageDomain();

describe('selectSettingsAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
