// import { selectSettingsAffiliationPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectSettingsAffiliationPageDomain();

describe('selectSettingsAffiliationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
