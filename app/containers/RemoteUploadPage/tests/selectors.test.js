// import { selectRemoteUploadPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectRemoteUploadPageDomain();
describe('selectRemoteUploadPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
