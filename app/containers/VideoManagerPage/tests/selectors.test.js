// import { selectVideoManagerPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectVideoManagerPageDomain();
describe('selectVideoManagerPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
