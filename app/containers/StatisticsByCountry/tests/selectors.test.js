// import { selectStatisticsByCountryDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectStatisticsByCountryDomain();
describe('selectStatisticsByCountryDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
