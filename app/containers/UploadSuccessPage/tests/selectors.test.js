// import { selectUploadSuccessPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectUploadSuccessPageDomain();
describe('selectUploadSuccessPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
