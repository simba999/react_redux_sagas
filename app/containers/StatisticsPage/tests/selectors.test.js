// import { selectStatisticsPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectStatisticsPageDomain();
describe('selectStatisticsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
