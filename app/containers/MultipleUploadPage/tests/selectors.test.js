// import { selectMultipleUploadPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from "expect";
// const selector = selectMultipleUploadPageDomain();
describe('selectMultipleUploadPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
